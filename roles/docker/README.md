Docker
=========

Install Docker Engine

Role variables
--------------

- `docker_firewall_backend`: Optional Docker firewall backend (for example `iptables` or `nftables`). Leave empty to keep Docker default.

License
-------

MIT
